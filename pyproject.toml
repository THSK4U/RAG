[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.10.0",
    "flake8>=7.0.0",
    "mypy>=1.8.0",
    "fire>=0.6.0",
    "tqdm>=4.66.0",
    "rank-bm25>=0.2.2",
    "transformers>=5.14.1",
    "torch>=2.13.0",
    "accelerate>=1.14.0",
    "huggingface-hub>=0.20.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
