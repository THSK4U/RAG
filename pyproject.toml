[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.10.0",
    "flake8>=7.0.0",
    "mypy>=1.8.0",
    "fire>=0.6.0",
    "tqdm>=4.66.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

